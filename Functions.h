#pragma once
#include "Attacks.h"
#include "DrawGUI.h"

bool hasEscaped();						// Checks to see if the player has managed to escape
void fightMonster(Player& player);		// Creates a fight between the player and a monster
